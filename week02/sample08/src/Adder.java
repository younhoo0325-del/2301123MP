import java.util.Scanner;
 //gg 2주차 자연수합 4 예제
public class Adder {
    private int start;
    private int last;


    public int readDate(String message){
        Scanner keyboard = new Scanner(System.in);
        int temp;
        while
        System.out.print(message);
        temp = keyboard.nextInt();
        if (temp >= 0)

    }

    @Override
    public String toString() {
        return String.format();
    }
}
